{
  "name": "@neststudio-usermods/ui-kit",
  "version": "0.3.0",
  "private": true,
  "license": "GPL-3.0-only",
  "description": "Renderer UI toolkit for Nest Studio user mods: toolbar buttons, popovers, modals, form and settings helpers (window.usermodUI)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w --preserveWatchOutput"
  },
  "devDependencies": {
    "@neststudio-usermods/types": "workspace:*"
  }
}