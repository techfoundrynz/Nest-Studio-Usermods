{
  "name": "@neststudio-usermods/asar",
  "version": "0.3.0",
  "private": true,
  "license": "GPL-3.0-only",
  "description": "Dependency-free asar extract/pack/list used to rebuild Nest Studio's app.asar",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": { "nest-asar": "dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w --preserveWatchOutput"
  }
}
