# pnpm workspace. Dependency build scripts are blocked by default (pnpm >= 10), which is exactly what we
# want for `electron`: it is a devDependency for its TypeScript declarations only, never the binary.
link-workspace-packages=true
