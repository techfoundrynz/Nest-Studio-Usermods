{
  "name": "@neststudio-mods/ui-scale",
  "version": "0.3.0",
  "private": true,
  "license": "GPL-3.0-only",
  "description": "UI zoom (Ctrl+= / Ctrl+- / Ctrl+0) and a compact density mode, remembered between sessions",
  "usermod": {
    "name": "ui-scale",
    "main": "dist/main.js",
    "ui": "dist/ui.js",
    "order": 25,
    "description": "UI zoom (Ctrl+= / Ctrl+- / Ctrl+0) and a compact density mode, remembered between sessions"
  },
  "scripts": {
    "build": "tsc -p tsconfig.main.json && tsc -p tsconfig.ui.json",
    "watch": "concurrently \"tsc -p tsconfig.main.json -w --preserveWatchOutput\" \"tsc -p tsconfig.ui.json -w --preserveWatchOutput\""
  },
  "devDependencies": {
    "@neststudio-usermods/types": "workspace:*"
  }
}