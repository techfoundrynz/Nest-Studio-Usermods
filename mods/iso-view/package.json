{
  "name": "@neststudio-mods/iso-view",
  "version": "0.3.0",
  "private": true,
  "license": "GPL-3.0-only",
  "description": "Isometric / orthographic-style view toggle and view presets for the 3D scene",
  "usermod": {
    "name": "iso-view",
    "ui": "dist/index.js",
    "order": 12,
    "description": "Isometric / orthographic-style view toggle and view presets for the 3D scene"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w --preserveWatchOutput"
  },
  "devDependencies": {
    "@neststudio-usermods/types": "workspace:*"
  }
}