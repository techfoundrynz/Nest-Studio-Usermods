{
  "name": "@neststudio-mods/z-probe",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "description": "Z touch-plate probing wizard (G38.2) that sets the work Z zero, with an optional re-probe at tool changes",
  "usermod": {
    "name": "z-probe",
    "ui": "dist/index.js",
    "order": 44,
    "description": "Z touch-plate probing wizard (G38.2) that sets the work Z zero, with an optional re-probe at tool changes"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w --preserveWatchOutput"
  },
  "devDependencies": {
    "@neststudio-usermods/types": "workspace:*",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:"
  }
}