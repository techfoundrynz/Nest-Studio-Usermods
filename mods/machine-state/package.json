{
  "name": "@neststudio-mods/machine-state",
  "version": "0.3.0",
  "private": true,
  "license": "GPL-3.0-only",
  "description": "Shared machine state (status, progress, ETA) broadcast to UI mods; optional console log to disk",
  "usermod": {
    "name": "machine-state",
    "main": "dist/index.js",
    "order": 5,
    "description": "Shared machine state (status, progress, ETA) broadcast to UI mods; optional console log to disk"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w --preserveWatchOutput"
  },
  "devDependencies": {
    "@neststudio-usermods/types": "workspace:*"
  }
}