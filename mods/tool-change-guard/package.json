{
  "name": "@neststudio-mods/tool-change-guard",
  "version": "0.3.0",
  "private": true,
  "license": "GPL-3.0-only",
  "description": "Safe retract, spindle/coolant off and optional M0 pause before every tool change",
  "usermod": {
    "name": "tool-change-guard",
    "postprocessor": "dist/index.js",
    "order": 18,
    "description": "Safe retract, spindle/coolant off and optional M0 pause before every tool change"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w --preserveWatchOutput"
  },
  "devDependencies": {
    "@neststudio-usermods/types": "workspace:*"
  }
}