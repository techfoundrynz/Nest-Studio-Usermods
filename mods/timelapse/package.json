{
  "name": "@neststudio-mods/timelapse",
  "version": "0.3.0",
  "private": true,
  "license": "GPL-3.0-only",
  "description": "Saves camera frames to data/timelapse while a job runs",
  "usermod": {
    "name": "timelapse",
    "main": "dist/index.js",
    "order": 50,
    "description": "Saves camera frames to data/timelapse while a job runs"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w --preserveWatchOutput"
  },
  "devDependencies": {
    "@neststudio-usermods/types": "workspace:*"
  }
}