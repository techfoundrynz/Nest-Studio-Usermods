{
  "name": "@neststudio-mods/toolpath-color",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "description": "Recolours the preview toolpaths by Z depth or per operation, and highlights the path under the mouse in the list",
  "usermod": {
    "name": "toolpath-color",
    "ui": "dist/index.js",
    "order": 48,
    "description": "Recolours the preview toolpaths by Z depth or per operation, and highlights the path under the mouse in the list"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w --preserveWatchOutput"
  },
  "devDependencies": {
    "@neststudio-usermods/types": "workspace:*",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:"
  }
}