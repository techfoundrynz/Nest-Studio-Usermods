{
  "name": "@neststudio-mods/lan-monitor",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "description": "Read-only status page served on the local network: machine status, progress, ETA and the latest camera frame",
  "usermod": {
    "name": "lan-monitor",
    "main": "dist/main.js",
    "ui": "dist/ui.js",
    "order": 62,
    "description": "Read-only status page served on the local network: machine status, progress, ETA and the latest camera frame"
  },
  "scripts": {
    "build": "tsc -p tsconfig.main.json && tsc -p tsconfig.ui.json",
    "watch": "concurrently \"tsc -p tsconfig.main.json -w --preserveWatchOutput\" \"tsc -p tsconfig.ui.json -w --preserveWatchOutput\""
  },
  "devDependencies": {
    "@neststudio-usermods/types": "workspace:*",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:"
  }
}