# compiled output (tsc) - the installer builds it
dist/

# node_modules (TypeScript + type declarations only)
node_modules/

# build output of tools/install.ps1 (extracted app + repacked archive, ~530 MB)
build/

# runtime artifacts
usermod.log
usermod.log.*
data/*
!data/.gitkeep

# editor / OS noise
.vscode/
Thumbs.db
*.tmp
